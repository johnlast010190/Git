noiseSourceDumps
{
    //note purgeWrite will delete fieldWrite dumps if they
    //coinside with restart dumps

    type fieldWrite;
    functionObjectLibs ( "libfieldFunctionObjects.so" );

    region region0;

    writeControl timeStep;//runTime, adjustableRunTime, cpuTime, clockTime
    writeInterval 10;

    spanControl startAndNwrites;//startAndEnd, start, end, none
    maxWrites 2000;
    start   11;
    end 1000000;


    fields
    (
        U //any valid vol or surface field
    );
}
