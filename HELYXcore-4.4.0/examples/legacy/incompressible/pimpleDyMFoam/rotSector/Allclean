#!/bin/bash
cd ${0%/*} || exit 1    # Run from this directory

# Source tutorial clean functions
. $HELYX_PROJECT_DIR/bin/tools/CleanFunctions

#------------------------------------------------------------------------------

# Clean
cleanCase && rm -rf 0/ *.vtk

# Clean stator
cd stator && cleanCase && rm -f *.vtk && cd -

# Remove system dictionaries
rm -rf system/boundaryConditions/ system/fa*

#------------------------------------------------------------------------------
