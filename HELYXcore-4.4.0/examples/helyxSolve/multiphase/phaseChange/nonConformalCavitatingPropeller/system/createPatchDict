/*--------------------------------*- C++ -*----------------------------------*\
|       o        |                                                            |
|    o     o     |  HELYX (R) : Open-source CFD for Enterprise                |
|   o   O   o    |  Version : 4.4.0                                           |
|    o     o     |  ENGYS Ltd. <http://engys.com/>                            |
|       o        |                                                            |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      createPatchDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// Do a synchronisation of coupled points after creation of any patches.
// Note: this does not work with points that are on multiple coupled patches
//       with transformations (i.e. cyclics).
pointSync false;

// Patches to create
patches
(
    {
        name            inlet;
        patchInfo
        {
            type        patch;
            physicalType inlet;
        }
        constructFrom   set;
        set             inletFaces;
    }

    {
        name            outlet;
        patchInfo
        {
            type        patch;
            physicalType outlet;
        }
        constructFrom   set;
        set             outletFaces;
    }

    {
        // Non-conformal owner patch
        name            nonCouple1;
        patchInfo
        {
            type        nonConformal;
            inGroups    (nonCouple);
        }
        constructFrom   patches;
        patches         (innerCylinder);
    }

    {
        // Non-conformal neighbour patch
        name            nonCouple2;
        patchInfo
        {
            type        nonConformal;
            inGroups    (nonCouple);
        }
        constructFrom   patches;
        patches         (innerCylinder_slave);
    }
);


// ************************************************************************* //
